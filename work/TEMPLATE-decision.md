# YYYY-MM-DD — <decision title>

- **Decision**: one sentence, in the imperative ("leave X disabled", "override soak gate for v0.6.17").
- **Why**: the evidence that forced it (link files, paste command output).
- **Reversal condition**: what observation would flip this decision.
- **Evidence**: run URLs, SHAs, tool output.

Three event classes MUST get an entry (CLAUDE.md rule): ring
promotions/qualifications/releases; anything touching the grail or its
runtime (launchd, tokens, push URLs); any "leave it broken/stale on
purpose" call.
